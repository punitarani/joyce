{
  "name": "server",
  "private": true,
  "scripts": {
    "build": "echo 'Can not build a Python package'",
    "dev": "uv run python main.py",
    "server": "uv run python main.py",
    "agent": "uv run python -m joyce.agent.run dev",
    "lint": "uv run ruff check --fix .",
    "format": "uv run ruff format .",
    "db:generate": "uv run alembic revision --autogenerate -m",
    "db:migrate": "uv run alembic upgrade head"
  }
}
